[pytest]
addopts = --html=reports/report.html --self-contained-html -v
markers =
    positivo: testes que verificam cenários válidos (espera sucesso)
    negativo: testes que verificam cenários inválidos (espera erro)
